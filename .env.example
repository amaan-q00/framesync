# ==============================================
#  FRAMESYNC — single source of env (dummy copy)
#  Copy to .env and fill values. Docker & backend read from root .env.
#  Next.js: NEXT_PUBLIC_API_URL is set by docker from API_URL; for local frontend dev set it in frontend/.env or match API_URL.
# ==============================================

# --- URLs (single place; no duplication) ---
# Backend API base (OAuth redirect_uri, CORS, cookie domain). Backend uses as BACKEND_PUBLIC_URL & FRONTEND_URL.
API_URL=http://localhost:8000
APP_URL=http://localhost:3000

# --- Database (Postgres) ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=framesync
DATABASE_URL=postgres://user:password@db:5432/framesync

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- Storage (MinIO / S3) ---
S3_ACCESS_KEY=admin
S3_SECRET_KEY=password123
S3_BUCKET=videos
S3_REGION=us-east-1
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_ENDPOINT=http://localhost:9000

# --- Auth ---
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=30d

# --- Google OAuth (backend-driven) ---
# Create OAuth 2.0 Client ID (Web application) at https://console.cloud.google.com/apis/credentials
# You get BOTH Client ID and Client Secret in the same popup when you create it (secret is shown once).
# If you only saved the Client ID: open the same credential in Credentials → click it → copy "Client secret" or use "Reset secret".
# Redirect URI to add in Console: {API_URL}/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Server (backend) ---
PORT=8000
NODE_ENV=development

# --- Cookie (backend) ---
COOKIE_SECURE=false
COOKIE_HTTPONLY=true
COOKIE_SAMESITE=strict
COOKIE_MAX_AGE=604800
