# ==============================================
#  FRAMESYNC — canonical env (single source)
#  Copy to .env. Backend builds DATABASE_URL from POSTGRES_*.
# ==============================================

# --- Where the app runs (one place: change port = backend port + URL + docker all follow) ---
# Local: set only these. API will be http://localhost:API_PORT, app at http://localhost:APP_PORT.
# Production: also set API_URL and APP_URL to your real URLs (e.g. https://api.example.com).
API_PORT=8000
APP_PORT=3000

# Production only: uncomment and set so OAuth and links use your real domain
# API_URL=https://api.yoursite.com
# APP_URL=https://app.yoursite.com

# --- Database (backend builds DATABASE_URL from these; use host=db in docker, localhost when running backend on host) ---
POSTGRES_HOST=db
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=framesync

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- Storage (MinIO/S3); S3_PUBLIC_ENDPOINT derived in code for browser URLs ---
S3_ACCESS_KEY=admin
S3_SECRET_KEY=password123
S3_BUCKET=videos
S3_REGION=us-east-1
S3_ENDPOINT=http://minio:9000

# --- Auth ---
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=7d

# --- Google OAuth (redirect URI in Console: {API_URL}/api/auth/google/callback) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Server ---
NODE_ENV=development

# --- Cookie (backend) ---
COOKIE_SECURE=false
COOKIE_HTTPONLY=true
COOKIE_SAMESITE=strict
COOKIE_MAX_AGE=604800

# --- Optional ---
VIDEO_RETENTION_HOURS=24


